# Task 1.4 — pre-move baseline capture

Captured after the gate-1 spike was fully torn down (task 1.3 confirmed the working tree back to a
clean pre-spike state) and before any of Group 2's oracle changes or Group 3's file moves. Every
number here is what later stages are measured against.

## Boundary checker — full output

```
$ node script/check-package-boundary.mjs
check-package-boundary: scanned 1031 repo file(s) (tracked + uncommitted)
  PASS  acyclic-direction: every cross-package edge points to a strictly lower declared layer (949 file(s) scanned, 341 cross-package edge(s) examined)
  PASS  public-entry-only: a specifier crossing into a package resolves only to a declared exports subpath (949 file(s) scanned, 0 @opencut/* specifier(s) examined)
  ....  no-internal-reexport: 0 files scanned — packages/ holds no source yet (no package's declared entry re-exports a module owned by another package's undeclared internals)
  PASS  no-elftia-import: no package, Host or example imports an Elftia package, protocol identifier or runtime object (1031 file(s) scanned)
  PASS  react-free-base: editor-ports and editor-contracts import no React, no DOM global, and no editor-classic module (68 file(s) scanned)

clean — run with --negative-control / --converse-control to see each rule proven able to fire, and proven not to misfire.
```

Matches the numbers documented in `tasks.md` 1.4 exactly: 341 cross-package edges, 949/949/1031/68
file counts, `public-entry-only` 0 specifiers examined, `no-internal-reexport` dormant.

### `--negative-control` — every rule proven able to fire

All 15 fixtures `PASS` (`clean — every rule is proven able to fail`), including the full D-1/D-2/D-4/
D-6/D-7/D-8/BLOCKER-1/MAJOR-1/MAJOR-2 regression-guard fixtures P0 accumulated across its three
review rounds. Full transcript not reproduced here — deterministic script output, re-runnable at
any time; see `script/check-package-boundary.mjs --negative-control`.

### `--converse-control` — no rule fires on a legal case

All 12 fixtures `PASS` (`clean — no rule fires on a legal case`). Same note: deterministic,
re-runnable via `script/check-package-boundary.mjs --converse-control`.

(The two controls are mutually exclusive on one invocation — `else if` in the script's argv
handling — so they must be run as two separate commands, not one `--negative-control
--converse-control` call.)

## Type baseline

```
$ node script/check-type-baseline.mjs
check-type-baseline: 3 diagnostic(s) now, 13 at the pin cf5e79e9
  compiler TypeScript 5.9.3, comparison key = file + code + message

Present at the pin, absent or reduced now:
  src/actions/keybindings/persistence.ts TS2345 (2 -> 0)
  src/actions/keybindings/persistence.ts TS2724 (1 -> 0)
  src/actions/keybindings/persistence.ts TS2724 (1 -> 0)
  src/services/storage/migrations/runner.ts TS2554 (1 -> 0)
  src/services/storage/migrations/runner.ts TS2554 (1 -> 0)
  src/services/storage/migrations/v1-to-v2.ts TS2554 (3 -> 0)
  src/stickers/providers/index.ts TS2554 (1 -> 0)
  Not a failure. Benign if a baseline defect was deliberately repaired; investigate if
  unexplained, because a shrunken type-check scope also looks like this.

PASS  no diagnostic outside the pinned baseline set
```

10 of the pin's 13 diagnostics are already resolved on current `HEAD`; 3 remain, all inside the pin
set. **0 new diagnostics.** The checker does not yet report how many files it type-checked — that
instrumentation is task 2.5's job, and its own control re-run (task 2.6) is what proves the file
count didn't quietly shrink when the packages get their own `tsconfig` wiring.

## PARITY.md header

```
9 difference(s): 0 semantic, 9 incidental. 195 leaf values compared.
```

From `PARITY.md` (repo root), generated by `script/diff-parity-snapshots.mjs` against
`apps/vite-example/tests/parity-artifacts/{vite,next}/snapshot-{vite,next}.json`. Matches the
number documented in `tasks.md` 1.4 exactly. This file is not regenerated by task 1.4 — it is
read as-is, since regenerating it requires the Playwright scenario, which is out of scope for a
static pre-move snapshot.

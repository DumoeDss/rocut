# Handoff: s0304-transaction-api-and-react-surface — LEAD #2

## Original intent

The user asked to read the existing handoff, enter the `rocut` development worktree, and continue the entire `$rasen-auto auto-decompose` portfolio end to end. They explicitly ruled that all roles use Codex only and that Claude Code must never be used. The latest request is `$rasen-handoff`, so this document pauses the run at a safe stage boundary.

## Position

Parent pipeline: `auto-decompose`; child pipeline: `small-feature`; Tier A Codex-native; gate policy `off (global)`. The parent portfolio is incomplete.

- Completed/recovered children: T0 `s0304-transaction-contract-freeze`, T1 `s0304-transaction-engine`, T2 `s0304-draft-editing-sessions`, and C1 `s0304-project-settings-transaction-operation`.
- T3 `s0304-ui-commit-routing`: `propose`, `apply`, `verify`, and `review-loop` are complete. Current frontier is `ship`, then `archive`.
- R0 `s0304-surface-embedding-contract-freeze`: `recovery-required`; its original local-only objects were lost. R1 is correctly blocked on R0.
- Pending after R0/T3: R1 `s0304-surface-mount-focus-lifecycle`, R2 `s0304-surface-css-react-a11y`, and T4 `s0304-agent-transaction-evidence`.
- Portfolio delivery remains pending and must happen once, only after all children complete.

Resume commands:

```powershell
rasen pipeline resume s0304-ui-commit-routing --project rocut --json
rasen pipeline resume s0304-transaction-api-and-react-surface --project rocut --json
```

Expected T3 result: `next=ship`, no open findings. Expected parent result: R0 and T3 are interrupted/in-progress; no dependent Surface child is runnable yet.

## Done / Remaining

Done:

- Canonical `rocut` Git repository is healthy on branch `recovery/s0304-ui-commit-routing-final`, HEAD `0f9b4ff5ef0c8e0a04522ce4369f47511a14dc47`, tree `9e2c8e08365bfb576c546bf5e0021b7642369442` before the live review-fix delta.
- T3's reconstructed implementation had 46/46 tasks, but the independent Codex review found 5 Blockers, 2 Majors, and 1 Minor. All eight were fixed by a separate non-author Codex fixer and confirmed CLEAN by a fresh non-author Codex reviewer: 0/0/0/0.
- T3 review evidence:
  - `rasen/changes/s0304-ui-commit-routing/evidence/review-report.md`
  - `rasen/changes/s0304-ui-commit-routing/evidence/review-cycle-report.md`
  - reviewed live product-delta SHA-256 `fe3ef159278fdeff901a83913485d4b08a07f04365f3a0127afbb95abd539882`
- Final focused gates passed: 22 tests / 177 expectations on the delta review; the fixer also ran the broader focused suite at 38 tests / 237 expectations. Changed-file ESLint, type baseline 3, transaction boundary, negative control, strict Rasen validation, live `git diff --check`, and Vite production build passed.
- R0 was independently reconstructed in read-only forensics. Its exact product bytes survive in the static transcript and reproduce the original product tree in memory.

Remaining:

1. T3 local-only ship: commit the current product/test/evidence delta without pushing or creating a PR, write `ship-log.md`, then update T3 and parent run-state.
2. T3 archive: run the normal `rasen-archive-change` stage after the local ship; do not infer archive timing from current config when the ship log exists.
3. R0 synthetic recovery: restore its planning artifacts and two exact product files, independently verify, local ship, and fresh archive. Do not claim recreation of the lost original commit identities.
4. Run R1, then R2, then T4, strictly serial for every `rocut`-mutating worker.
5. Perform one parent-level portfolio delivery only after every child is terminal.

## Key decisions (and why)

- All planner/implementer/reviewer/fixer/shipper roles are Codex. Never invoke Claude Code or a Claude runtime; static historical Claude transcript files may be read as recovery evidence only.
- Serialize every `rocut` writer. The shared checkout already suffered a cross-branch race and later a destructive Git-for-Windows worktree-removal incident.
- Child delivery mode is always `local`; never push a partial portfolio.
- Author != verifier is mandatory. T3's original implementer did not verify its own work; the round-1 fixer and delta reviewer were distinct Codex agents.
- A1 remains `(a)`: R0 exposes an opaque `commit({ edit: unknown })` seam; R1 consumes T0's transaction types. Do not pull transaction/domain types into R0.
- A2 remains shared React 18. R2 implements/proves it; isolated React 19 previously failed with React #321 at the router-context seam.
- Preserve T3 invariants: explicit stable `routingClass` independent of minified `constructor.name`; non-boolean audio `hidden` normalizes to `false`; first-image canvas uses typed `update-project`; baseline `pushHistory:false` owns undo; durable-before-publication, one-root/one-save, and exact projection remain non-negotiable.
- R0 archive must be freshly generated by Rasen after fresh verification. Do not hand-forge the old nondeterministic archive metadata.

## Dead ends & gotchas

- Original local Git objects `f2e36b9b`, `3978d724`, `552f15a1`, `aac84ff1`, `27e4e8d2`, R0 `fab202d4`, and R0 archive `e94ea055` are absent locally and remotely. Direct fetches already failed; do not repeat them.
- Never run `git worktree remove --force` against the preserved Windows reparse-point layout. That command removed the original `.git` and tracked roots. Do not delete or move the accident backups.
- Preserved backup roots:
  - `E:/AI/ChatAI/Agents/VibeCodingProjects/elftia/_others/rocut-damaged-backup-20260810-codex`
  - `E:/AI/ChatAI/Agents/VibeCodingProjects/elftia/_others/rocut-git-recovery`
  - `E:/AI/ChatAI/Agents/VibeCodingProjects/elftia/_others/rocut-t3-evidence-backup-20260810-codex`
- Do not clean `.rasen/` or unrelated untracked Rasen material.
- Two restored Codex subagent sessions repeatedly failed at dispatch with `Selected model is at capacity`. Fresh Codex-native recovery agents completed the fix and re-review; these were infra revivals, not review rounds or handoff relays.
- The dependency tree was initially partial; the fixer repaired it with `bun install --frozen-lockfile`. Focused tests, ESLint, and type baseline then ran successfully.
- Next build compiles but page-data collection remains blocked by unset build-time variables (`NEXT_PUBLIC_MARBLE_API_URL`, `DATABASE_URL`, `BETTER_AUTH_SECRET`, plus service credentials). Do not invent or expose secrets. Vite is the reliable production-build gate.
- `git diff 4b7af6f2..HEAD --check` is polluted by pre-existing CRLF/whitespace in unrelated recovered archives. The live delta and both exact F8 paths are clean; use scoped evidence rather than rewriting unrelated archives.

## Eliminated hypotheses

- T3's 46/46 task ticks were sufficient proof of correctness — ruled out by the independent review's five reproducible Blockers. The current live delta closes all eight findings and has non-author confirmation.
- T3 needed another review round — ruled out: round 1 re-review is CLEAN at 0 Blocker / 0 Major / 0 Minor / 0 Trivial.
- R0 product bytes were irrecoverable — ruled out: two transcript `Write.input.content` payloads reproduce their Git blobs and the exact original product tree.
- R0's original commit/archive identities could be recreated safely — ruled out: product bytes are exact, but complete commit headers and nondeterministic archive metadata are unavailable. Use synthetic commits and record provenance honestly.

## Working set

Repository: `E:/AI/ChatAI/Agents/VibeCodingProjects/elftia/_others/rocut`

Authoritative run-state:

- Parent: `.rasen/changes/s0304-transaction-api-and-react-surface/ephemera/portfolio-run.json`
- T3: `.rasen/changes/s0304-ui-commit-routing/ephemera/auto-run.json`

Current T3 live delta is intentionally uncommitted. Modified product/test files:

- `apps/web/src/actions/use-editor-actions.ts`
- `apps/web/src/commands/index.ts`
- `apps/web/src/commands/media/remove-media-asset.ts`
- `apps/web/src/core/managers/__tests__/media-persistence-rewire.test.ts`
- `apps/web/src/core/managers/__tests__/transaction-command-routing.test.ts`
- `apps/web/src/core/managers/commands.ts`
- `apps/web/src/core/managers/media-manager.ts`
- `apps/web/src/core/managers/timeline-manager.ts`
- `apps/web/src/editor/transactions/opencut/{adapter,draft-context,index,projection,router,routing}.ts`

New product files:

- `apps/web/src/commands/provider-private-composite.ts`
- `apps/web/src/editor/transactions/opencut/history-rebase.ts`

F8/evidence changes:

- `rasen/changes/archive/2026-08-09-s0304-transaction-contract-freeze/evidence/review-cycle-report.md`
- `rasen/specs/transaction-automation-api/spec.md`
- `rasen/changes/s0304-ui-commit-routing/evidence/review-report.md`
- `rasen/changes/s0304-ui-commit-routing/evidence/review-cycle-report.md`

Pre-existing untracked `.rasen/` must remain untouched. A shipper should explicitly stage the intended files rather than `git add -A` across unrelated artifacts.

R0 exact reconstruction source:

- Static transcript: `C:/Users/Sayo/.claude/projects/E--AI-ChatAI-Agents-VibeCodingProjects-elftia-elftia-elftia/432c1542-4f82-4b60-9f4f-9661c69cec61/subagents/agent-aimpl-r0-58df05918476176e.jsonl`
- `types.ts`: record 79; SHA-256 `191d8ce880dc4807c90f8ff9113333c4e6992d1e4274bcc756a62a6cad3d5379`; blob `5bbfc48d3a1dc0af71275ce1dccad71df0f2d167`.
- `index.ts`: record 83; SHA-256 `81a514fa12be1e5d19d25d816f13de3a9122de61771df1de0c1abe6c844cb313`; blob `bfbf6f6fdb54813d5c9d252cbb5bdc0ca385e57a`.
- Reconstructed base-plus-two-files tree must equal `3e1cce7fc0e95e4221d1911b558167408198378a`.
- Planning/archive artifact bodies survive in the planner and archive transcripts documented by the previous agents; original R0 archive contained nine `embeddable-react-surface` requirements.

## Next action

Dispatch a fresh Codex-native shipper for `s0304-ui-commit-routing`. It must read `rasen-ship` completely, use explicit `local` mode, stage only the intended T3 product/test/evidence and two F8 files, confirm 46/46 tasks and CLEAN review evidence, reuse or refresh the scoped gates as required by tree identity, create the local commit and `ship-log.md`, and perform no push/PR/archive. Then let the LEAD update run-state and dispatch a separate Codex archive worker.

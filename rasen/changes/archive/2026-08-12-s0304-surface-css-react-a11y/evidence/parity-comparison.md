# Parity deviation report

One Playwright scenario — create a project, import an image, a video and two audio files, place clips on two visual and two audio tracks, drag, trim, split, snap, scrub, play, save, reload the page fully, reopen — driven against **two production builds of the same editor source**: the Vite example with no Next.js runtime, and `apps/web` under `next build` + `next start`. After the scenario each run reads the persisted project out of IndexedDB through the storage service's own database and store names and normalizes it.

Normalization replaces ids with ordinals by first appearance, timestamps with placeholders and data-URL payloads with a mime placeholder. It never touches track membership, clip ordering, `startTime`, `duration`, `trimStart` or `trimEnd` — those are what the diff exists to compare.

This report is evidence, not a verdict: it does not decide whether the Slice passed.

Generated by `script/diff-parity-snapshots.mjs` from the two normalized snapshots:

- Vite host: `apps/vite-example/tests/parity-artifacts/vite/snapshot-vite.json`
- Next host: `apps/vite-example/tests/parity-artifacts/next/snapshot-next.json`

**28 difference(s): 19 semantic, 9 incidental.** 275 leaf values compared.

Classification is fail-safe: anything without an explicit incidental rule is reported as semantic.

## Semantic differences

| path | vite | next |
| --- | --- | --- |
| `project.__opencutTransaction.idempotency[0].fingerprint` | ["array",[["object",[["kind",["string","update-project"]],["patch",… | ["array",[["object",[["kind",["string","update-project"]],["patch",… |
| `project.__opencutTransaction.idempotency[0].key` | opencut-ui:2763cf6c-fd88-4ce6-b916-fd3c4f39e7fc | opencut-ui:1b508532-be80-4df8-936a-af9d94cc390c |
| `project.__opencutTransaction.idempotency[0].result.createdIds[0]` | <id:4> | <id:2> |
| `project.__opencutTransaction.idempotency[0].result.createdIds[1]` | <id:2> | <id:3> |
| `project.__opencutTransaction.idempotency[0].result.createdIds[2]` | <id:3> | <id:4> |
| `project.__opencutTransaction.idempotency[1].fingerprint` | ["array",[["object",[["kind",["string","create-track"]],["track",["… | ["array",[["object",[["kind",["string","create-track"]],["track",["… |
| `project.__opencutTransaction.idempotency[1].key` | opencut-ui:bf836f8a-d262-419a-9b55-82480a5649c3 | opencut-ui:16378047-365e-4de2-98cb-eeb3b857ad9d |
| `project.__opencutTransaction.idempotency[2].fingerprint` | ["array",[["object",[["kind",["string","create-track"]],["track",["… | ["array",[["object",[["kind",["string","create-track"]],["track",["… |
| `project.__opencutTransaction.idempotency[2].key` | opencut-ui:d9b459e8-f703-4403-ae6b-1326939a95bc | opencut-ui:89731491-b372-478f-a961-c2269d819447 |
| `project.__opencutTransaction.idempotency[3].fingerprint` | ["array",[["object",[["kind",["string","create-track"]],["track",["… | ["array",[["object",[["kind",["string","create-track"]],["track",["… |
| `project.__opencutTransaction.idempotency[3].key` | opencut-ui:9836a2a3-3a88-4151-bb0c-3e9abe2baa54 | opencut-ui:727990ae-d159-4a32-b3e1-23be5aea6298 |
| `project.__opencutTransaction.idempotency[4].fingerprint` | ["array",[["object",[["clipId",["string","88da1667-9357-47e5-8b05-d… | ["array",[["object",[["clipId",["string","328c09cc-8962-4bce-9a0a-0… |
| `project.__opencutTransaction.idempotency[4].key` | opencut-ui:8da95783-991a-47db-91a4-b28cc2fe0f1a | opencut-ui:62b5499b-031c-4343-984e-1bd3f167ade1 |
| `project.__opencutTransaction.idempotency[5].fingerprint` | ["array",[["object",[["clipId",["string","3c896f99-d359-4dcf-8b12-0… | ["array",[["object",[["clipId",["string","e2151a11-9a4b-4d5f-9524-1… |
| `project.__opencutTransaction.idempotency[5].key` | opencut-ui:ddabeec0-0c77-49bd-8f69-0013eff871dd | opencut-ui:954f4a9e-98d1-4c1e-9713-0b5c76779481 |
| `project.__opencutTransaction.idempotency[6].fingerprint` | ["array",[["object",[["clip",["object",[["assetId",["string","2675d… | ["array",[["object",[["clip",["object",[["assetId",["string","70098… |
| `project.__opencutTransaction.idempotency[6].key` | opencut-ui:a9b26ac8-95a3-49a1-905c-b2ecb3f05a72 | opencut-ui:70845000-9775-4e5e-b7f8-e0a10c5b2ab5 |
| `project.__opencutTransaction.idempotency[7].fingerprint` | ["array",[["object",[["clipId",["string","14ec842e-e2cc-4099-980f-a… | ["array",[["object",[["clipId",["string","6f97c0e9-f2e7-4c64-b663-0… |
| `project.__opencutTransaction.idempotency[7].key` | opencut-ui:10bcf978-1a6b-4b95-bf09-9b2480452c72 | opencut-ui:c38dddea-1e9e-44e6-b8d0-2390be800ac0 |

**Each row above is a Slice finding.** A semantic difference means the same editing actions produced different persisted state in the two hosts.

## Incidental differences

| path | vite | next | why it carries no editing meaning |
| --- | --- | --- | --- |
| `project.metadata.duration` | 656000 | 652000 | Project duration is the end of the last clip, so it inherits the one-frame difference on the placement values rather than being an independent deviation. |
| `project.metadata.name` | Untitled Project | New project | The project name is chosen by the host's own create call (`Untitled Project` in the Vite example, `New project` in apps/web), not by the editor. |
| `project.scenes[0].tracks.audio[0].elements[0].startTime` | 208000 | 212000 | Exactly one frame (4,000 ticks at 30 fps). The scenario's drag and trim are **pixel**-denominated gestures, and the two hosts hand the timeline different widths on purpose — the Vite example embeds the editor in a bounded box rather than the viewport — so their zoom differs (7.13 vs 7.31, 2.5%). Both hosts *request* the same pixel distance, but neither the achieved distance nor the ticks it converts to are the same: a gesture is measured in pixels and stored in ticks at that host's own zoom, then quantized to a frame. **Only an exact one-frame difference is classified here; anything larger stays semantic** — but the rule matches on value, not on cause; see "Limits of this classification". |
| `project.scenes[0].tracks.audio[1].elements[0].duration` | 208000 | 212000 | Exactly one frame (4,000 ticks at 30 fps). The scenario's drag and trim are **pixel**-denominated gestures, and the two hosts hand the timeline different widths on purpose — the Vite example embeds the editor in a bounded box rather than the viewport — so their zoom differs (7.13 vs 7.31, 2.5%). Both hosts *request* the same pixel distance, but neither the achieved distance nor the ticks it converts to are the same: a gesture is measured in pixels and stored in ticks at that host's own zoom, then quantized to a frame. **Only an exact one-frame difference is classified here; anything larger stays semantic** — but the rule matches on value, not on cause; see "Limits of this classification". |
| `project.scenes[0].tracks.audio[1].elements[0].trimEnd` | 32000 | 28000 | Exactly one frame (4,000 ticks at 30 fps). The scenario's drag and trim are **pixel**-denominated gestures, and the two hosts hand the timeline different widths on purpose — the Vite example embeds the editor in a bounded box rather than the viewport — so their zoom differs (7.13 vs 7.31, 2.5%). Both hosts *request* the same pixel distance, but neither the achieved distance nor the ticks it converts to are the same: a gesture is measured in pixels and stored in ticks at that host's own zoom, then quantized to a frame. **Only an exact one-frame difference is classified here; anything larger stays semantic** — but the rule matches on value, not on cause; see "Limits of this classification". |
| `project.scenes[0].tracks.overlay[0].elements[0].startTime` | 56000 | 52000 | Exactly one frame (4,000 ticks at 30 fps). The scenario's drag and trim are **pixel**-denominated gestures, and the two hosts hand the timeline different widths on purpose — the Vite example embeds the editor in a bounded box rather than the viewport — so their zoom differs (7.13 vs 7.31, 2.5%). Both hosts *request* the same pixel distance, but neither the achieved distance nor the ticks it converts to are the same: a gesture is measured in pixels and stored in ticks at that host's own zoom, then quantized to a frame. **Only an exact one-frame difference is classified here; anything larger stays semantic** — but the rule matches on value, not on cause; see "Limits of this classification". |
| `project.scenes[0].tracks.overlay[0].elements[1].startTime` | 356000 | 352000 | Exactly one frame (4,000 ticks at 30 fps). The scenario's drag and trim are **pixel**-denominated gestures, and the two hosts hand the timeline different widths on purpose — the Vite example embeds the editor in a bounded box rather than the viewport — so their zoom differs (7.13 vs 7.31, 2.5%). Both hosts *request* the same pixel distance, but neither the achieved distance nor the ticks it converts to are the same: a gesture is measured in pixels and stored in ticks at that host's own zoom, then quantized to a frame. **Only an exact one-frame difference is classified here; anything larger stays semantic** — but the rule matches on value, not on cause; see "Limits of this classification". |
| `project.timelineViewState.playheadTime` | 244000 | 240000 | Timeline zoom/scroll state is derived from the timeline's pixel width. The Vite example deliberately embeds the editor in a bounded box rather than the whole viewport, so the two hosts give it different widths. |
| `project.timelineViewState.zoomLevel` | 7.13 | 7.31 | Timeline zoom/scroll state is derived from the timeline's pixel width. The Vite example deliberately embeds the editor in a bounded box rather than the whole viewport, so the two hosts give it different widths. |

## Limits of this classification

**The one-frame rule matches on value, not on cause.** A difference is classified incidental when the path ends in `startTime`, `duration`, `trimStart` or `trimEnd` and the two values differ by exactly 4,000 ticks. The rule states a zoom explanation but never checks it: it does not derive the expected delta from each host's recorded pixel distance and zoom. A genuine one-frame regression on exactly the values this diff exists to protect would therefore be absorbed and reported as a pass.

Everything else is caught. Against deliberately corrupted snapshots, shifting a placement by two frames, deleting a clip, reordering clips, moving a clip to another track, deleting a whole track, tampering with a trim, injecting a `blob:` URL and removing a media asset all report as semantic and exit non-zero. The exact one-frame delta is the single blind spot, and it is inherent to the rule rather than incidental to a run.

Closing it means making the rule causation-aware — reconciling each difference against the interaction ledger's recorded pixel distance at that host's zoom, and rejecting a 4,000-tick delta the gesture does not account for. That is a harness change, deliberately not made here.

A second, smaller limit: the `why` column is prose written by whoever added the rule. It explains a classification; nothing re-derives it at run time.

## Track summary, side by side

| host | visual tracks | audio tracks | clips (name@start+duration, in track order) |
| --- | --- | --- | --- |
| vite | 2 | 2 | main:video: fixture-image.png@0+600000<br>overlay:video: fixture-video.mp4 (left)@56000+300000, fixture-video.mp4 (right)@356000+300000<br>audio: fixture-tone-a5.wav@208000+360000<br>audio: fixture-tone-a4.wav@0+208000 |
| next | 2 | 2 | main:video: fixture-image.png@0+600000<br>overlay:video: fixture-video.mp4 (left)@52000+300000, fixture-video.mp4 (right)@352000+300000<br>audio: fixture-tone-a5.wav@212000+360000<br>audio: fixture-tone-a4.wav@0+212000 |

Times are in wasm `MediaTime` ticks (120,000 per second), which is what the editor persists.

## Interaction ledger

Per interaction, per host: **asserted** (a machine-checked claim held), **captured** (a screenshot exists but nothing was asserted), or **missing** (neither — not evidence, and reported as absent).

| interaction | vite | next | assertions |
| --- | --- | --- | --- |
| `create-open` — Create and open a project | asserted (2) | asserted (2) | editor chrome (main track + timecode) is on screen; a project record exists in video-editor-projects |
| `import-media` — Import image, video and two audio assets | asserted (2) | asserted (2) | all four assets appear in the assets panel; all four assets are persisted in media-metadata with their names |
| `place-multi-track` — Place clips on >=2 visual and >=2 audio tracks | asserted (1) | asserted (1) | 2 visual and 2 audio tracks carry clips (persisted) |
| `drag` — Drag a clip along the timeline | asserted (2) | asserted (2) | clip moved 166px right (requested 160px); persisted startTime is now 56000 (was 0) |
| `trim` — Trim a clip by its resize handle | asserted (2) | asserted (2) | clip width 713px -> 618px; persisted trim: duration=208000 trimStart=0 trimEnd=32000 |
| `split` — Split a clip at the playhead | asserted (1) | asserted (1) | clips derived from fixture-video.mp4: 1 -> 2 |
| `snap` — Snap a clip to a neighbouring clip edge | asserted (2) | asserted (2) | the snap indicator was rendered during the drag; the clip landed on the neighbour edge, not where the pointer released |
| `scrub` — Scrub the playhead on the ruler | asserted (1) | asserted (1) | the displayed timecode advances monotonically with ruler position |
| `play` — Play the preview and pause it | asserted (2) | asserted (2) | playback advances the timecode; pausing holds the timecode still |
| `save-reload-reopen` — Save, full page reload, reopen with equivalent content | asserted (1) | asserted (1) | after a full reload the reopened project has identical tracks, clip order, placement and trims |

**Third-party requests blocked during the runs: 1.** Blocked: https://cdn.databuddy.cc/databuddy.js.

**Console errors across both runs: 2.** Failed to load resource: net::ERR_FAILED | Failed to load resource: net::ERR_FAILED


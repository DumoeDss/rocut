# C6 integration handoff

The accepted C6 product is integrated at `a9dbae62573af5877b440ecf9cb2b8a0d4f1dbbf`, tree `885d307814260b77397c2c2677b9361fdfc5f5e2`. The cherry-pick was conflict-free and the integrated tree exactly matches the shipped child tree.

Fresh integrated lifecycle/resource/storage/static/type/provenance gates and independent Vite/Next build/browser controls are recorded in `evidence/c6-integration-a9dbae62-20260805.md`. Both Hosts pass ordinary six-cycle and durable-reopen controls and reject missing-created and deliberate-leak controls. Owned ports 4212 and 3212 are free. The one storage aggregate red was the inherited Bun 1.2.2 isolated-child segfault; exact immediate single-file replay passed.

The main `session-resource-disposal` spec is synchronized after integration. Its 14 requirements and 59 scenarios match the delta body exactly, all 14 main specs validate strictly, and the earlier 13-spec two-way sweep remains preserved. Tasks are now 128 checked / 9 unchecked / 137 total. The remaining unchecked historical RED/prerequisite leaves are intentionally not fabricated; 14.7 and 14.8 belong to the next separate archive leaf.

Do not clean generated outputs. The integration worktree intentionally retains uniquely marked Vite/Next builds and generated probe records as untracked evidence. Archive only the planning change and main spec, validate strictly, preserve all evidence/handoffs, commit locally, and do not push or modify product code.

The first two separately persisted Luna-xhigh archive launches were stopped before any repository action: one by a PowerShell stderr-launcher defect and the corrected one by the external Codex usage limit. Exact thread identities and the unchanged-tree ruling are recorded in `evidence/c6-archive-deferred-luna-xhigh-20260805.md`. Tasks 14.7/14.8 remain unchecked; do not substitute another model for the required archive leaf.

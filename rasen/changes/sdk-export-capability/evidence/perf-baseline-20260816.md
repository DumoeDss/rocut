# Export performance baseline — 2000-clip dense project (2026-08-16)

Generated by `apps/electron-host/scripts/export-perf-baseline.mjs` (do not hand-edit; the script overwrites this file on every run).
This is the measurement, not a gate pass: the 2000-clip acceptance gate is S08's to set — every "vs S08 expectation" cell states the metric's standing without a pass/fail claim.

## Machine facts

| fact | value |
| --- | --- |
| CPU | AMD Ryzen 7 5800H with Radeon Graphics          x 16 |
| total RAM | 63.3 GiB |
| OS | win32 10.0.26200 (x64) |
| node | v24.14.0 |
| bun | 1.2.2 |
| electron | 43.4.0 |
| playwright | 1.62.0 |
| ffmpeg | ffmpeg version 6.0-full_build-www.gyan.dev Copyright (c) 2000-2023 the FFmpeg developers |
| scratch drive free space | 27100.2 MB |
| GPU | none — `--use-angle=swiftshader --enable-unsafe-swiftshader` (software GL); every rendering number below is CPU-rendered |
| harness command | `node apps/electron-host/scripts/export-perf-baseline.mjs --runs 2` |

## Project shape and why

`Perf 2000 Dense`: 2000 clips (1000 text + 1000 graphic) on 16 overlay tracks, dense layout, canvas 1280x720, fps 30/1, ~10 s timeline (300 frames).
The dense layout is the deliberate choice for disk feasibility: every frame composites all 2000 elements, and the raw export stream (~830 MB transient at 720p) fits the E: scratch disk. The staggered layout (the long sparse ~250 s timeline — the timeline-UI stress shape) is the S08 follow-up, not measured here.

Generator METHOD block (verbatim from the generator's stdout, run 1):

```
command inputs: --root E:\AI\ChatAI\Agents\VibeCodingProjects\elftia\_others\rocut-export-scratch\perf-baseline-2026-08-16T15-24--run1\store --clips 2000 --name "Perf 2000 Dense" --layout dense --width 1280 --height 720 (defaults: clips 2000, name "Perf 2000", layout staggered, 1920x1080)
store classes: FilesystemProjectStore over NodeFsStoreBridge (../src/store/*), identity DEFAULT_FILESYSTEM_STORE_IDENTITY, schemaVersion read off the store instance (31); record built in the encodeProject durable shape (dates ISO strings, ELEMENT_KEYS/TRACK_KEYS with undefined for absent fields)
determinism: mulberry32(seed=clips), no Math.random; element ids `clip-<i zero-padded 6>`; project/scene/track ids uuidFromRng drawn in order: project, scene, main track, 16 overlay tracks; dates = Date.UTC(2026,0,1) + rng()*86400000 (updated = created + rng()*3600000; scene drawn separately)
project: one main scene "Main scene"; settings: fps 30/1, canvas 1280x720 (overridden, canvasSizeMode "custom"), background {color #000000}; version 31
tracks: main video track "Main Track" EMPTY (no media pipeline), 0 audio tracks; overlay = 8 text tracks ("Text 1".."Text 8") + 8 graphic tracks ("Graphic 1".."Graphic 8")
elements: 2000 total = 1000 text + 1000 graphic
layout formula (dense): element i (0-based) -> overlay track (i % 16), slot floor(i / 16); startTime = slot * 1920 ticks (all of a track's elements start within its first 2s), duration 960000 (8s), trimStart 0, trimEnd 0 — every element composites in every frame (TICKS_PER_SECOND = 120000)
text element (i % 16 < 8): name/content "Clip <i>", fontFamily Arial, fontSize 15+(i%5)*10, color PALETTE[i%6] (#ff5252 #4dff88 #4d9fff #ffd24d #c14dff #ffffff), full DEFAULTS.text.param set (background.*, letterSpacing 0, lineHeight 1.2)
graphic element (i % 16 >= 8): definitionId "rectangle" (builtin registry), fill PALETTE[(i+3)%6], stroke #000000, strokeWidth i%3, strokeAlign "center", cornerRadius (i%4)*10, scale 0.5+((i*7)%10)/10
motion (both kinds): transform.positionX (i*97)%1600-800, transform.positionY (i*131)%800-400, transform.rotate ((i*13)%360)-180, opacity 1, blendMode "normal" — frames differ over time by construction
totals: timeline = 1198080 ticks (9.984s); elements per track = 125 or 125 (last track takes remainder)
rng draws consumed: 308
project id: fc379315-3d76-4274-b52d-1673a7a17264
```

## Definitions and substitutions (read before quoting any number)

- Cold open, interactions, playback and export methods are defined inside each run's section; all in-page deltas use the renderer's `performance.now()` clock, cold open uses the harness monotonic clock, and no two clocks are ever subtracted across.
- The timeline zoom buttons carry no testid/aria-label, so interaction A locates the zoom-in button structurally: the last direct-child `<button>` of the toolbar group containing the Radix `[role="slider"]` (app code untouched).
- The export panel is always mounted beside the editor, so interaction C measures reopen-after-collapse (collapse via its aria-labeled button, reopen via `[data-testid=export-panel-toggle]`).
- Export phase boundaries come from in-page MutationObserver timestamps of the panel's status text ("Starting…" / "Rendering frames…" / "Encoding…" / "Export complete."), not from main-side instrumentation; the 250 ms driver poll only builds the progress series and the raw-size peak. If the page dies mid-export (the 2026-08-16 pre-fix run lost the renderer to a GPU-process crash), the boundaries fall back to the driver poll series and the output file's mtime, labeled as derived — a crash is recorded as the run's export outcome, never as a missing cell.
- The dense layout overlaps elements within a track (8 s durations starting 16 ms apart), so a click point's topmost element need not be the one the app's hit-testing selects: interaction B observes the whole clicked track row and verifies selection by the resize handles, reporting which element actually ended up selected.
- The project id is identical across runs (deterministic seed); each run still gets a fresh store root and fresh exports root, both removed by the harness on exit.

## Run 1 — 2026-08-16T15:24:17.958Z

- route into the editor: page.goto opencut://app/index.html?project=<id>
- store root: `E:\AI\ChatAI\Agents\VibeCodingProjects\elftia\_others\rocut-export-scratch\perf-baseline-2026-08-16T15-24--run1\store` (removed after the run)
- exports root: `E:\AI\ChatAI\Agents\VibeCodingProjects\elftia\_others\rocut-export-scratch\perf-baseline-2026-08-16T15-24--run1\exports` (removed after the run)
- launch env keys passed: APPDATA, COMMONPROGRAMFILES, COMSPEC, HOMEDRIVE, HOMEPATH, LOCALAPPDATA, NUMBER_OF_PROCESSORS, OPENCUT_EXPORT_ROOT, OPENCUT_FFMPEG_PATH, OPENCUT_STORE_ROOT, OS, PATH, PATHEXT, PROCESSOR_ARCHITECTURE, PROCESSOR_IDENTIFIER, PROGRAMFILES, ProgramData, SYSTEMDRIVE, SYSTEMROOT, TEMP, TMP, USERPROFILE, WINDIR

### Cold open

Definition: editor interactive = [data-testid=export-panel] present AND button[aria-label='Select Main Track track'] present AND all 2000 element nodes (div.absolute.top-0.select-none) mounted; harness monotonic clock from before electron.launch().

| marker (since launch) | time | vs S08 expectation |
| --- | --- | --- |
| electron.launch() resolved | 961 ms | no S08 gate exists yet for 2000-clip cold open — baseline recorded for S08 to set against |
| first window acquired | 961 ms | no S08 gate exists yet for 2000-clip cold open — baseline recorded for S08 to set against |
| page.goto(?project=) resolved | 1315 ms | no S08 gate exists yet for 2000-clip cold open — baseline recorded for S08 to set against |
| export-panel present in DOM | 1754 ms | no S08 gate exists yet for 2000-clip cold open — baseline recorded for S08 to set against |
| main-track select button present | 9562 ms | no S08 gate exists yet for 2000-clip cold open — baseline recorded for S08 to set against |
| timecode present | 9562 ms | no S08 gate exists yet for 2000-clip cold open — baseline recorded for S08 to set against |
| all 2000 element nodes mounted | 9562 ms | no S08 gate exists yet for 2000-clip cold open — baseline recorded for S08 to set against |
| **Cold open total (launch → editor interactive)** | 9562 ms | no S08 gate exists yet for 2000-clip cold open — baseline recorded for S08 to set against |
| poll-stable end (4 consecutive equal counts) | 12.59 s | no S08 gate exists yet for 2000-clip cold open — baseline recorded for S08 to set against |

Element node count at end of polling: **2000** (expected 2000).

### Interaction latency (event → observable effect)

| interaction | event → effect wall time | effect verified | vs S08 expectation |
| --- | --- | --- | --- |
| A — timeline zoom-in button click | 598 ms | verified | no S08 gate exists yet for interaction latency — baseline recorded |
| B — click a clip on the timeline (select) | **failed**: click produced no observable selection effect (no resize handles appeared anywhere in the timeline) | NOT verified | no S08 gate exists yet for interaction latency — baseline recorded |
| C — reopen the export panel | 1 ms | verified | no S08 gate exists yet for interaction latency — baseline recorded |

Markers (recorded per definition, in-page performance.now clock):
- A start: trusted click event on the timeline toolbar zoom-in button (in-page capture listener)
- A effect: first inline width style mutation on the timeline tracks scroll content (MutationObserver, in-page) (width 1449.88px → 1772.14px)
- B start: trusted click at (267, 553) (in-page capture listener); topmost element under the point: "Rectangle", row has 125 elements
- B effect: no selection effect observed (no row mutation, no resize handles anywhere in the timeline)
- B marker used: none · no element ended up selected
- C start: trusted click on [data-testid=export-panel-toggle] after collapsing the panel (in-page capture listener)
- C effect: aside[data-testid=export-panel] present in DOM again (MutationObserver on body, in-page)

### Playback frame rate

Method: in-page requestAnimationFrame counter over a 5 s window during playback started with the Space key (keybinding 'space' -> toggle-play), main-track button focused first so the surface-root keydown listener receives it.

| metric | value | vs S08 expectation |
| --- | --- | --- |
| playback started (timecode advanced) | yes (00:00:00:00 → 00:00:09:29) | — |
| frames observed in 5 s window | 5 | — |
| measured window | n/a | — |
| **observed fps** | n/a | not measured |
| median frame interval | 7 ms | — |
| dropped-frame heuristic count | 2 (rAF intervals > 1.5x the median interval) | — |

### Export wall time, split by phase

Request: format mp4, quality medium, include audio false (the project has no audio; the audio phase is D1's territory).
Method: phase boundaries = in-page MutationObserver timestamps of distinct [data-testid=export-status] texts (renderer performance.now clock); progress series = 250 ms driver poll.
**Outcome: no terminal event within budget (last status: "Rendering frames…" after 900.11 s).**

| measure | value | vs S08 expectation |
| --- | --- | --- |
| click → render start (queued) | 1332 ms | no S08 gate exists yet — baseline recorded |
| **render phase (first rendering → first encoding)** | n/a | no S08 gate exists yet for 2000-clip render wall time — baseline recorded for S08 to set |
| **encode phase (first encoding → completed)** | n/a | no S08 gate exists yet — baseline recorded |
| **total export wall time (click → completed)** | n/a | no S08 gate exists yet — baseline recorded |
| terminal panel status | Rendering frames… | — |
| frames (panel text, final live sample) | 28% — 84/299 frames | — |
| output file | none | — |
| output bytes | n/a | — |
| output verified then deleted | no | — |
| raw stream peak (250 ms fs poll, jobs/*.raw) | 221.5 MB (analytic expectation 791.0 MB) | — |
| progress samples taken | 3177 @ 250 ms | — |
| job snapshot (jobs/*.json, main-side) | phase rendering, progress 0.2809364548494983 | — |

Status-event series (in-page timestamps, renderer clock):
- Starting… @ +81592 ms
- Rendering frames… @ +82741 ms

Progress series tail (250 ms driver poll, live samples only):
- +898764 ms: Rendering frames… · 28% · raw 221.5 MB
- +899034 ms: Rendering frames… · 28% · raw 221.5 MB
- +899311 ms: Rendering frames… · 28% · raw 221.5 MB
- +899579 ms: Rendering frames… · 28% · raw 221.5 MB
- +899839 ms: Rendering frames… · 28% · raw 221.5 MB

### Memory

Method: renderer usedJSHeapSize via performance.memory (page.evaluate) + main-process RSS via process.memoryUsage().rss (app.evaluate in main); 'before' sampled after playback pause, 'after' 2 s past export completion (or past the recorded export outcome).

| sample | renderer usedJSHeapSize | main-process RSS | vs S08 expectation |
| --- | --- | --- | --- |
| before export | 193.9 MB | 139.6 MB | no S08 gate exists yet for memory — baseline recorded |
| after export (+2 s settle) | 251.3 MB | 141.3 MB | no S08 gate exists yet for memory — baseline recorded |
| delta | 57.5 MB | 1.6 MB | — |

performance.memory available: **true**.

### Observations (app-level, run 1)

- onboarding dialog seen and dismissed: none
- element nodes mounted: 2000
- renderer console errors: 67
  - `Failed to render preview frame: RuntimeError: unreachable     at opencut://app/assets/opencut_wasm_bg-k0ttTfDM.wasm:wasm-function[4277]:0x2a8ccf     at opencut://app/assets/opencut_wasm_bg-k0ttTfDM.wasm:wasm-function[3783]:0x29af04     at opencut://app/assets/opencut_wasm_bg-k0ttTfDM.wasm:wasm-funct`
  - `Failed to render preview frame: RuntimeError: unreachable     at opencut://app/assets/opencut_wasm_bg-k0ttTfDM.wasm:wasm-function[3304]:0x291df7     at Uj (opencut://app/assets/electron-host-config-vAuaubrp.js:2664:19)     at lA (opencut://app/assets/electron-host-config-vAuaubrp.js:583:31)     at o`
  - `panicked at /cargo\registry\src\index.crates.io-1949cf8c6b5b557f\parking_lot_core-0.9.12\src\thread_parker\wasm.rs:26:9: Parking not supported on this platform  Stack:  Error     at aL (opencut://app/assets/electron-host-config-vAuaubrp.js:1179:16)     at opencut://app/assets/opencut_wasm_bg-k0ttTfD`
  - `Failed to render preview frame: RuntimeError: unreachable     at opencut://app/assets/opencut_wasm_bg-k0ttTfDM.wasm:wasm-function[1952]:0x25e839     at opencut://app/assets/opencut_wasm_bg-k0ttTfDM.wasm:wasm-function[3399]:0x293221     at opencut://app/assets/opencut_wasm_bg-k0ttTfDM.wasm:wasm-funct`
  - `Failed to render preview frame: RuntimeError: unreachable     at opencut://app/assets/opencut_wasm_bg-k0ttTfDM.wasm:wasm-function[3304]:0x291df7     at Uj (opencut://app/assets/electron-host-config-vAuaubrp.js:2664:19)     at lA (opencut://app/assets/electron-host-config-vAuaubrp.js:583:31)     at o`
- main-process error-ish output lines (tail): 1
  - `[stderr] [23072:0816/233437.822:ERROR:content\browser\gpu\gpu_process_host.cc:1089] GPU process exited unexpectedly: exit_code=34`

## Run 2 — 2026-08-16T15:40:43.266Z

- route into the editor: page.goto opencut://app/index.html?project=<id>
- store root: `E:\AI\ChatAI\Agents\VibeCodingProjects\elftia\_others\rocut-export-scratch\perf-baseline-2026-08-16T15-40--run2\store` (removed after the run)
- exports root: `E:\AI\ChatAI\Agents\VibeCodingProjects\elftia\_others\rocut-export-scratch\perf-baseline-2026-08-16T15-40--run2\exports` (removed after the run)
- launch env keys passed: APPDATA, COMMONPROGRAMFILES, COMSPEC, HOMEDRIVE, HOMEPATH, LOCALAPPDATA, NUMBER_OF_PROCESSORS, OPENCUT_EXPORT_ROOT, OPENCUT_FFMPEG_PATH, OPENCUT_STORE_ROOT, OS, PATH, PATHEXT, PROCESSOR_ARCHITECTURE, PROCESSOR_IDENTIFIER, PROGRAMFILES, ProgramData, SYSTEMDRIVE, SYSTEMROOT, TEMP, TMP, USERPROFILE, WINDIR

### Cold open

Definition: editor interactive = [data-testid=export-panel] present AND button[aria-label='Select Main Track track'] present AND all 2000 element nodes (div.absolute.top-0.select-none) mounted; harness monotonic clock from before electron.launch().

| marker (since launch) | time | vs S08 expectation |
| --- | --- | --- |
| electron.launch() resolved | 1075 ms | no S08 gate exists yet for 2000-clip cold open — baseline recorded for S08 to set against |
| first window acquired | 1075 ms | no S08 gate exists yet for 2000-clip cold open — baseline recorded for S08 to set against |
| page.goto(?project=) resolved | 1407 ms | no S08 gate exists yet for 2000-clip cold open — baseline recorded for S08 to set against |
| export-panel present in DOM | 1860 ms | no S08 gate exists yet for 2000-clip cold open — baseline recorded for S08 to set against |
| main-track select button present | 11.85 s | no S08 gate exists yet for 2000-clip cold open — baseline recorded for S08 to set against |
| timecode present | 11.85 s | no S08 gate exists yet for 2000-clip cold open — baseline recorded for S08 to set against |
| all 2000 element nodes mounted | 11.85 s | no S08 gate exists yet for 2000-clip cold open — baseline recorded for S08 to set against |
| **Cold open total (launch → editor interactive)** | 11.85 s | no S08 gate exists yet for 2000-clip cold open — baseline recorded for S08 to set against |
| poll-stable end (4 consecutive equal counts) | 15.88 s | no S08 gate exists yet for 2000-clip cold open — baseline recorded for S08 to set against |

Element node count at end of polling: **2000** (expected 2000).

### Interaction latency (event → observable effect)

| interaction | event → effect wall time | effect verified | vs S08 expectation |
| --- | --- | --- | --- |
| A — timeline zoom-in button click | 829 ms | verified | no S08 gate exists yet for interaction latency — baseline recorded |
| B — click a clip on the timeline (select) | **failed**: click produced no observable selection effect (no resize handles appeared anywhere in the timeline) | NOT verified | no S08 gate exists yet for interaction latency — baseline recorded |
| C — reopen the export panel | 1 ms | verified | no S08 gate exists yet for interaction latency — baseline recorded |

Markers (recorded per definition, in-page performance.now clock):
- A start: trusted click event on the timeline toolbar zoom-in button (in-page capture listener)
- A effect: first inline width style mutation on the timeline tracks scroll content (MutationObserver, in-page) (width 1449.88px → 1772.14px)
- B start: trusted click at (267, 553) (in-page capture listener); topmost element under the point: "Rectangle", row has 125 elements
- B effect: no selection effect observed (no row mutation, no resize handles anywhere in the timeline)
- B marker used: none · no element ended up selected
- C start: trusted click on [data-testid=export-panel-toggle] after collapsing the panel (in-page capture listener)
- C effect: aside[data-testid=export-panel] present in DOM again (MutationObserver on body, in-page)

### Playback frame rate

Method: in-page requestAnimationFrame counter over a 5 s window during playback started with the Space key (keybinding 'space' -> toggle-play), main-track button focused first so the surface-root keydown listener receives it.

| metric | value | vs S08 expectation |
| --- | --- | --- |
| playback started (timecode advanced) | yes (00:00:00:00 → 00:00:09:29) | — |
| frames observed in 5 s window | 2 | — |
| measured window | 5937 ms | — |
| **observed fps** | 0.3 | 0.3 fps on swiftshader — the S08 gate does not exist yet; today's number is the baseline S08 must set against |
| median frame interval | 417 ms | — |
| dropped-frame heuristic count | n/a (n/a — fewer than 3 frames observed in the window, the >1.5x-median heuristic is undefined at this frame rate (not zero)) | — |

### Export wall time, split by phase

Request: format mp4, quality medium, include audio false (the project has no audio; the audio phase is D1's territory).
Method: in-page observer unreachable (page died mid-export) — phase boundaries derived from the 250 ms driver poll series and output-file mtime (driver/wall clock, coarser); salvage detail below.
**Outcome: app died during export (page gone, no output produced).**

| measure | value | vs S08 expectation |
| --- | --- | --- |
| click → render start (queued) | 1281 ms (derived — driver poll / output-file mtime clock) | no S08 gate exists yet — baseline recorded |
| **render phase (first rendering → first encoding)** | n/a | no S08 gate exists yet for 2000-clip render wall time — baseline recorded for S08 to set |
| **encode phase (first encoding → completed)** | n/a | no S08 gate exists yet — baseline recorded |
| **total export wall time (click → completed)** | n/a | no S08 gate exists yet — baseline recorded |
| terminal panel status | Rendering frames… | — |
| frames (panel text, final live sample) | 8% — 24/299 frames | — |
| output file | none | — |
| output bytes | n/a | — |
| output verified then deleted | no | — |
| raw stream peak (250 ms fs poll, jobs/*.raw) | 63.3 MB (analytic expectation 791.0 MB) | — |
| progress samples taken | 1094 @ 250 ms | — |
| job snapshot (jobs/*.json, main-side) | phase rendering, progress 0.0802675585284281 | — |
| page death during export | window/page closed (Playwright page 'close' event) at +315.96 s (driver clock) | app-level finding — reported, not fixed |

Salvage detail (driver-side filesystem polling after the page died):
- page poll failed at +317.02 s (driver clock): page.evaluate: Target page, context or browser has been closed
- window/page closed (Playwright page 'close' event) at +315.96 s (driver clock)
- main process alive: no (app.evaluate failed: electronApplication.evaluate: Target page, context or browser has been closed)
- no filesystem activity (raw/partial/output bytes) for 180 s at +497.03 s — the pipeline stopped with the page (180 s > one slow swiftshader frame, so this is a stall, not a slow frame)

Status-event series (in-page timestamps, renderer clock):
- _none reachable — the in-page observer died with the page; use the derived boundaries in the table above_

Progress series tail (250 ms driver poll, live samples only):
- +314508 ms: Rendering frames… · 8% · raw 63.3 MB
- +314796 ms: Rendering frames… · 8% · raw 63.3 MB
- +315099 ms: Rendering frames… · 8% · raw 63.3 MB
- +315472 ms: Rendering frames… · 8% · raw 63.3 MB
- +315752 ms: Rendering frames… · 8% · raw 63.3 MB

### Memory

Method: renderer usedJSHeapSize via performance.memory (page.evaluate) + main-process RSS via process.memoryUsage().rss (app.evaluate in main); 'before' sampled after playback pause, 'after' 2 s past export completion (or past the recorded export outcome).

| sample | renderer usedJSHeapSize | main-process RSS | vs S08 expectation |
| --- | --- | --- | --- |
| before export | 206.3 MB | 138.7 MB | no S08 gate exists yet for memory — baseline recorded |
| after export (+2 s settle) | not sampled — the page died during export (see export outcome) | not sampled — the main process was gone (see export outcome) | no S08 gate exists yet for memory — baseline recorded |
| delta | not sampled — the page died during export (see export outcome) | not sampled — the main process was gone (see export outcome) | — |

performance.memory available: **true**.

### Observations (app-level, run 2)

- onboarding dialog seen and dismissed: none
- element nodes mounted: 2000
- renderer console errors: 66
  - `Failed to render preview frame: RuntimeError: unreachable     at opencut://app/assets/opencut_wasm_bg-k0ttTfDM.wasm:wasm-function[4277]:0x2a8ccf     at opencut://app/assets/opencut_wasm_bg-k0ttTfDM.wasm:wasm-function[3783]:0x29af04     at opencut://app/assets/opencut_wasm_bg-k0ttTfDM.wasm:wasm-funct`
  - `Failed to render preview frame: RuntimeError: unreachable     at opencut://app/assets/opencut_wasm_bg-k0ttTfDM.wasm:wasm-function[3304]:0x291df7     at Uj (opencut://app/assets/electron-host-config-vAuaubrp.js:2664:19)     at lA (opencut://app/assets/electron-host-config-vAuaubrp.js:583:31)     at o`
  - `panicked at /cargo\registry\src\index.crates.io-1949cf8c6b5b557f\parking_lot_core-0.9.12\src\thread_parker\wasm.rs:26:9: Parking not supported on this platform  Stack:  Error     at aL (opencut://app/assets/electron-host-config-vAuaubrp.js:1179:16)     at opencut://app/assets/opencut_wasm_bg-k0ttTfD`
  - `Failed to render preview frame: RuntimeError: unreachable     at opencut://app/assets/opencut_wasm_bg-k0ttTfDM.wasm:wasm-function[1952]:0x25e839     at opencut://app/assets/opencut_wasm_bg-k0ttTfDM.wasm:wasm-function[3399]:0x293221     at opencut://app/assets/opencut_wasm_bg-k0ttTfDM.wasm:wasm-funct`
  - `panicked at /cargo\registry\src\index.crates.io-1949cf8c6b5b557f\parking_lot_core-0.9.12\src\thread_parker\wasm.rs:26:9: Parking not supported on this platform  Stack:  Error     at aL (opencut://app/assets/electron-host-config-vAuaubrp.js:1179:16)     at opencut://app/assets/opencut_wasm_bg-k0ttTfD`
- main-process error-ish output lines (tail): 0

## Repeatability

Both runs measured the same deterministic project (same generator seed → same project id) against fresh scratch roots; deltas below are run-to-run on this machine.

| metric | run 1 | run 2 | delta |
| --- | --- | --- | --- |
| cold open total (ms) | 9562 ms | 11849 ms | 2288 ms |
| interaction A zoom (ms) | 598 ms | 829 ms | 232 ms |
| interaction B select (ms) | n/a | n/a | n/a |
| interaction C panel (ms) | 1 ms | 1 ms | 0 ms |
| export render phase (ms) | n/a | n/a | n/a |
| export encode phase (ms) | n/a | n/a | n/a |
| export total (ms) | n/a | n/a | n/a |
| raw peak (MB) | 221.5 | 63.3 | 158.2 MB |

All 2 run(s) completed with every metric family measured.

App-level export finding: 2 of 2 run(s) did not complete their export — run 1: no terminal event within budget (last status: "Rendering frames…" after 900.11 s); run 2: app died during export (page gone, no output produced). Measured and reported, not fixed (the harness does not touch app code).


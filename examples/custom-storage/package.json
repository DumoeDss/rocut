{
	"name": "opencut-example-custom-storage",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "Bring your own storage: the alien adapter's own ProjectStore behind a deliberately alien representation, driven through every published conformance surface — plus the migration honest pair (production path records its skip distinctly; the real chain validated through the published wasm test mock).",
	"scripts": {
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@opencut/editor-ports": "0.2.0",
		"@opencut/editor-contracts": "0.3.0",
		"@opencut/editor-classic": "0.2.0"
	},
	"devDependencies": {
		"@types/bun": "^1.2.15",
		"@types/node": "^22.10.0",
		"typescript": "^5.8.3"
	},
	"opencutExample": {
		"bunEntries": ["run.ts", "run-mock.ts"]
	}
}

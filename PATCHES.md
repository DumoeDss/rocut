# Patch log

Every local modification to a file inherited from upstream OpenCut Classic
(`cf5e79e919144200294fb9fed22a222592a0aeea`) is recorded here. The pinned upstream commit must stay
identifiable as the baseline, so a reader can reconstruct exactly what this fork changed and why.

> **This table must be appended to by every task that modifies an upstream file — as the change is
> made, not retroactively.** A modified upstream file with no row here is a defect. New files added
> by this fork are not patches and are not listed; see `UPSTREAM.md` for the added-file inventory.

Verify the set of modified files against the pin with:

```
git diff --name-only cf5e79e919144200294fb9fed22a222592a0aeea -- apps rust script
```

Columns:

- **id** — stable patch identifier (`P-nnn`), referenced from code comments where useful.
- **upstream path** — repo-relative path of the inherited file.
- **change** — what was actually altered.
- **rationale** — why, including the Slice spec clause that forces it. A patch with no forcing
  clause is scope creep.
- **verification** — how the patch was confirmed not to change behaviour (or what behaviour change
  was accepted).

| id | upstream path | change | rationale (+ forcing clause) | verification |
| --- | --- | --- | --- | --- |
| P-001 | `apps/web/next.config.ts` | Added `typescript: { ignoreBuildErrors: true }`. | The pinned baseline does not type-check, so `next build` fails at the type gate and no production Next build exists. §3.3 requires a production `next build` of `apps/web` as the **parity reference** the Vite output is compared against, and §3.2 keeps `apps/web` buildable as the live regression check. Skipping the type gate restores that reference without editing any editor source — the alternative (fixing the ~6 upstream type defects) would silently repair the baseline this Slice exists to compare against. See `UPSTREAM.md` § "Known upstream defects" for the full error list. | `next build` compiles successfully both before and after (the failure was in the post-compile type pass only), so emitted output is unaffected. Types are erased by SWC and never reach the bundle. |
| P-002 | `.gitignore` | Added `.rasen-store/`, `rust/wasm/pkg/`, `apps/vite-example/dist/` and the three parity-run output directories `apps/vite-example/tests/{parity-artifacts,.pw-output,.playwright}/`. **Amended (ruling R10):** the `.rasen-store/` comment previously claimed "`rasen/` itself stays tracked on this branch", which was false — this worktree has no `rasen/` directory. The comment now states what is true: the planning artifacts live in the main checkout as untracked local state, and this branch carries code plus the provenance documents. **Amended again (review MAJ-2):** the parity rule covered `parity-artifacts/` and `.pw-output/` but not `.playwright/`, an earlier output location for the same run, leaving 14 files there committable. | `.rasen-store/` is a machine-local store pointer (design D13, as corrected by R10); `rust/wasm/pkg/` is `wasm-pack` build output written by the §3.1 rebuild-correspondence check; `apps/vite-example/dist/` is the Vite production build; the three `tests/` directories are parity-run output, regenerated by `bun run test:parity`. None belongs in version control — the committed outcome of a parity run is `PARITY.md`. Upstream ignores `.next/` in `apps/web/.gitignore` but had no rule covering a second app's build output, because no second app existed. | Repository hygiene, and one correctness consequence. Without the `dist/` rule `git status` listed 303 untracked build artifacts (~34 MB, including the 21.6 MB ONNX wasm) as committable. The missing `.playwright/` rule was worse than hygiene: `tests/.playwright/next/snapshot-next.json` was a **superseded** Next snapshot predating the media sort-by-name normalization in `snapshot.ts`, named identically to the real one and one directory away from it, and a valid input to the documented diff command — run against it the differ reports 39 differences / 27 semantic (exit 1), the opposite verdict to the committed `PARITY.md`. The directory has been deleted as well as ignored, and the differ's own usage docstring, which still cited the stale `.playwright/` path, now cites `parity-artifacts/`. Verified after the fix: `git check-ignore -v` matches all three rules, and no path matching `parity-artifacts|.playwright|.pw-output|snapshot-|ledger-|results-` appears in `git ls-files --cached --others --exclude-standard`. `main` is untouched. |
| P-003 | `apps/web/src/components/editor/panels/assets/views/assets.tsx` | `next/image` → `HostImage` (2 call sites, both `fill` + `unoptimized`). | §3.2 requires the production editor bundle to contain no Next runtime import. | Both sites already passed `unoptimized`, so Next was already serving the raw `src`. `fill` is emulated with `position:absolute; inset:0; width:100%; height:100%`, matching what Next's `fill` sets. Rendering is unchanged in both hosts. |
| P-004 | `apps/web/src/stickers/components/assets-view.tsx` | `next/image` → `HostImage`. | §3.2, as P-003. | Call site already passed `unoptimized`. It also passes `onError`, which `HostImage` forwards natively by extending `ImgHTMLAttributes` — the sticker load-failure fallback still works. |
| P-005 | `apps/web/src/timeline/components/timeline-element.tsx` | `next/image` → `HostImage` (2 call sites). | §3.2, as P-003. | Both already passed `unoptimized`; fixed `width`/`height`, no `fill`. |
| P-006 | `apps/web/src/guides/definitions/platforms.tsx` | `next/image` → `HostImage`. | §3.2, as P-003. | Already passed `unoptimized`. Source is the remote `cdn.brandfetch.io`, which stays a §3.7 diagnostic-only dependency; removing Next's `remotePatterns` gate does not change what loads. |
| P-007 | `apps/web/src/components/editor/editor-header.tsx` | Removed `next/image`, `next/link` and `next/navigation`. Logo → `HostImage`; Discord `<Link>` → plain external `<a>`; `router.push("/projects")` (exit **and** delete flows) → `navigation.onExitProject()`; `FeedbackPopover` now renders only when `services.feedbackEndpoint` is set. Also removed `@/site/brand` and `@/site/social`: the logo URL now comes from `branding.logoUrl` and the Discord URL from `links.discordUrl`. | §3.2 (no Next import; navigation via callbacks; **site code absent from the distributable graph**) and §3.7 (server-route features are host-configured and degrade visibly). | The logo is an SVG, which Next does not optimize, so the image swap is a no-op. The Discord link was already `target="_blank"` to an external URL — `next/link` added nothing. The Next host supplies `feedbackEndpoint: "/api/feedback"` and passes the same `DEFAULT_LOGO_URL` / `SOCIAL_LINKS.discord` values through the host object, so `apps/web` renders identically to the pin. |
| P-008 | `apps/web/src/components/providers/editor-provider.tsx` | Removed `next/navigation`. `projectId` now comes from the host context instead of a prop; `router.replace(\`/editor/${id}\`)` → `navigation.onProjectReplaced({ projectId })`; the three full-page states changed from `h-screen w-screen` to `size-full`. | §3.2 — project identity and navigation must enter through props/callbacks rather than route globals, and the editor must mount inside its root container. | The not-found → create-new-project recovery path is preserved; only the notification mechanism changed. The `window.__wasmPanic` read and the error/loading states are untouched. The Next host's `onProjectReplaced` calls `router.replace` with the identical URL, so `apps/web` behaviour is unchanged. |
| P-009 | `apps/web/src/components/editor/mobile-gate.tsx` | Removed `next/link`, `next/navigation` and `@/site/brand`. `router.back()` → `navigation.onGoBack()`; roadmap `<Link href="/roadmap">` → external `<a href={roadmapUrl}>` with the URL supplied by `links.roadmapUrl`; `h-screen w-screen` → `size-full`. | §3.2, as P-008, plus the same site-code exclusion as P-007. | The Next host's `onGoBack` calls `router.back()`, so `apps/web` is unchanged. The Next host builds `roadmapUrl` as `` `${SITE_URL}/roadmap` ``, the exact string this file previously computed inline. The link becomes absolute so it resolves from a host with no `/roadmap` route; in `apps/web` it now leaves for the public site rather than navigating in-app — a deliberate, recorded behaviour change. |
| P-010 | `apps/web/src/app/editor/[project_id]/page.tsx` | Reduced to a thin Next host: reads `useParams`/`useRouter`, builds the `EditorHost` value, supplies the `h-screen w-screen` wrapper, and renders `MobileGate` → `EditorProvider` → `EditorRoot` plus `ChangelogNotification`. Everything else moved to the new `editor/surface/editor-root.tsx`. Also became the single place that reads `@/site/brand` and `@/site/social` for the editor, feeding them in as `branding` / `links`. | §3.2 — the editor root must not own the browser page, the Next-only shell must be separable from editor behaviour, and site code must stay out of the distributable graph. | Same component tree, same order, same classes; the viewport wrapper moved out one level. `ChangelogNotification` stays in the Next host, so `apps/web` loses nothing and the Vite graph never reaches `content-collections`. The site constants keep one source of truth — they moved consumer, not value. |
| P-015 | `bun.lock` | Regenerated by `bun install` after `apps/vite-example` was added to the workspace: a new `apps/vite-example` workspace entry plus its resolved dependency closure (Vite 7, `@vitejs/plugin-react`, `vite-plugin-wasm`, `vite-plugin-top-level-await`, and later `@playwright/test` 1.62.0 for the parity scenario). Also drops the top-level `"configVersion": 1` key, which the locally installed bun 1.2.2 does not write. | §3.6 — the developer path must be reproducible from a clean checkout, which requires the example's dependencies to be locked rather than floating. | No existing resolution changed: the pre-existing package set is byte-identical apart from the new entries, and both the `apps/web` production build and the Vite production build succeed against it. Re-verified from a clean state: with `node_modules` deleted, `bun install` restored 994 packages in 52 s **without rewriting the lockfile**. **The `configVersion` removal is an artefact of the bun version discrepancy** (installed 1.2.2 vs the `packageManager` pin 1.2.18) recorded in `UPSTREAM.md` § Toolchain, not a deliberate change; re-running `bun install` under 1.2.18 is expected to restore it. |
| P-014 | `apps/web/src/components/editor/onboarding.tsx` | Removed `@/site/social`; the Discord invite in the final onboarding step now comes from `links.discordUrl`. | §3.2 — site code must be absent from the distributable editor graph. `Onboarding` is rendered by `EditorRoot`, so it is inside that graph. **This import was missed by the phase-1 scan**, which found only `editor-header.tsx` and `mobile-gate.tsx`. | Same URL, same markdown link text; the Next host passes `SOCIAL_LINKS.discord` through unchanged. The hardcoded `https://opencut.app/roadmap` literal in step 1's prose is upstream content, not a module import, and is deliberately left alone — it does not affect the module-graph boundary. |
| P-011 | `apps/web/src/sounds/use-sound-search.ts` | Endpoint now read from `services.soundSearchEndpoint` instead of the hardcoded `/api/sounds/search`; both fetch paths skip entirely when it is unset, surfacing `SOUND_SEARCH_UNAVAILABLE_MESSAGE`. | §3.7 — server-route-dependent features must be host-configured and degrade with a visible diagnostic, never a misleading one. | The Next host supplies the same `/api/sounds/search` string, so `apps/web` issues byte-identical requests. Gating on configuration rather than request failure is required because a static host answers a missing API route with its SPA fallback at HTTP 200. |
| P-012 | `apps/web/src/sounds/components/assets-view.tsx` | Same endpoint gating for the top-sounds fetch, plus an explicit unavailable state rendered in the panel body. | §3.7, as P-011. | **Design D9 assumed an existing `setError` display path; there is none** — the store's `error` field was written but never read by any component. An explicit render was therefore added rather than reused. `apps/web` is unaffected because its endpoint is always configured. |
| P-013 | `apps/web/src/feedback/components/feedback-popover.tsx` | Posts to `services.feedbackEndpoint` instead of the hardcoded `/api/feedback`, and no-ops when unset. | §3.7, as P-011. Belt-and-braces: `EditorHeader` already hides the control without an endpoint. | The Next host supplies the same `/api/feedback` string, so `apps/web` behaviour is identical. |
